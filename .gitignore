# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Local config & secrets — examples stay committed, real values never do
.env
config/config.yaml

# Runtime data (SQLite, logs) — root only (do not ignore Android `…/mimir/data/`)
/data/

# PyInstaller (TUI exe)
build/
dist/
*.spec

# OS junk
Thumbs.db
.DS_Store
