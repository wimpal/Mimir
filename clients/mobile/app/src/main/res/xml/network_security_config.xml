<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- v1 LAN: brain runs on http://<PC-LAN-IP>:8000 (no TLS on home LAN). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
